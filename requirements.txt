# --- Core deep learning stack ---
# NOTE: pin these before final submission (see below) -- unpinned versions
# undermine the reproducibility claims made in Chapter 3.
torch
torchaudio
# torchvision  # not used anywhere in this codebase; remove unless something needs it

# --- ASR models / backends ---
openai-whisper
faster-whisper
ctranslate2
nemo_toolkit[asr]      # required by bench/adapters/parakeet_adapter.py -- was missing

# --- Hugging Face stack (Wav2Vec2, MINDS-14 loading, metrics) ---
transformers
datasets
evaluate

# vosk   # removed: Vosk dropped from ADAPTER_REGISTRY; re-add only if you
#          need the package installed to inspect/reproduce old Vosk results

# --- Data handling / metrics ---
pandas
numpy
scipy
jiwer                  # confirm this, not `evaluate`'s WER metric, is what
                        # bench/utils.py:compute_wer() actually uses

# --- Utilities ---
tqdm
matplotlib
psutil

# --- Audio I/O ---
soundfile
librosa

# openpyxl  # no .xlsx output found in runner/adapters; keep only if used
#             for manual dataset curation outside the benchmark pipeline

silero-vad              # PyPI convention uses a hyphen; pip normalizes
                         # underscore/hyphen equivalently, so this is
                         # cosmetic, not a functional fix
